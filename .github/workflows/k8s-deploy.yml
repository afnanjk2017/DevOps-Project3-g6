name: deploy to kubernetes

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Choose environment"
        required: true
        default: "dev"

jobs:
  deploy-k8s:
    name: "Deploy all Kubernetes manifests"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the branch
        uses: actions/checkout@v5

      - name: Azure Login (Service Principal)
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDS }}

      - name: Debug Terraform Outputs
        run: |
          echo "RG NAME: ${{ steps.aks_output.outputs.rg_name }}"
          echo "AKS NAME: ${{ steps.aks_output.outputs.aks_name }}"

      - name: HashiCorp - Setup Terraform
        uses: hashicorp/setup-terraform@v3.1.2

      - name: Terraform Init
        working-directory: ./infra
        run: |
          terraform init \
            -backend-config="resource_group_name=${{ secrets.STORAGE_RG }}" \
            -backend-config="storage_account_name=${{ secrets.STORAGE_AC }}" \
            -backend-config="container_name=${{ secrets.BLOB_CON }}" \
            -backend-config="key=${{ secrets.keytf }}"

      - name: Get AKS Details from Terraform Output
        id: aks_output
        working-directory: ./infra
        run: |
          echo "rg_name=$(terraform output -raw rg_name)" >> $GITHUB_OUTPUT
          echo "aks_name=$(terraform output -raw aks_name)" >> $GITHUB_OUTPUT
          echo "db_server_fqdn=$(terraform output -raw db_server_fqdn)" >> $GITHUB_OUTPUT
          echo "db_name=$(terraform output -raw db_name)" >> $GITHUB_OUTPUT
          echo "db_admin=$(terraform output -raw db_admin)" >> $GITHUB_OUTPUT
          echo "dns_zone_name=$(terraform output -raw dns_zone_name)" >> $GITHUB_OUTPUT
          echo "dns_zone_rg=$(terraform output -raw dns_zone_rg)" >> $GITHUB_OUTPUT
          echo "dns_subdomain=$(terraform output -raw dns_subdomain)" >> $GITHUB_OUTPUT

      - name: Connect to AKS Cluster
        run: |
          az aks get-credentials \
            --resource-group ${{ steps.aks_output.outputs.rg_name }} \
            --name ${{ steps.aks_output.outputs.aks_name }} \
            --overwrite-existing

      - name: Prepare namespace and DB secret
        run: |
          set -e
          kubectl get ns dev || kubectl create ns dev
          JDBC_URL="jdbc:sqlserver://${{ steps.aks_output.outputs.db_server_fqdn }}:1433;database=${{ steps.aks_output.outputs.db_name }};encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;"
          kubectl -n dev delete secret db-credentials || true
          kubectl -n dev create secret generic db-credentials \
            --from-literal=DB_URL=${JDBC_URL} \
            --from-literal=DB_USERNAME=${{ steps.aks_output.outputs.db_admin }} \
            --from-literal=DB_PASSWORD=${{ secrets.DB_PASSWORD }}

      - name: Render templates
        run: |
          set -e
          sudo apt-get update && sudo apt-get install -y gettext-base
          export DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
          export APP_DOMAIN="${{ steps.aks_output.outputs.dns_subdomain }}.${{ steps.aks_output.outputs.dns_zone_name }}"
          envsubst < k8s/backend/backend-deploy.yml > /tmp/backend-deploy.yml
          envsubst < k8s/frontend/ui-deploy.yml > /tmp/ui-deploy.yml
          envsubst < k8s/backend/backend-ingress.yml > /tmp/backend-ingress.yml
          envsubst < k8s/frontend/frontend-ingress.yml > /tmp/frontend-ingress.yml

      - name: Apply backend resources
        run: |
          set -e
          kubectl apply -f k8s/backend/backend-svc.yml -n dev
          kubectl apply -f /tmp/backend-deploy.yml -n dev
          kubectl apply -f /tmp/backend-ingress.yml -n dev
          kubectl apply -f k8s/backend/backend-hpa.yml -n dev

      - name: Apply frontend resources
        run: |
          set -e
          kubectl apply -f k8s/frontend/ui-configmap.yml -n dev
          kubectl apply -f k8s/frontend/frontend-clusterip.yml -n dev
          kubectl apply -f /tmp/ui-deploy.yml -n dev
          kubectl apply -f /tmp/frontend-ingress.yml -n dev
          kubectl apply -f k8s/frontend/frontend-hpa.yml -n dev

      - name: Map domain to Ingress IP (Azure DNS A record)
        run: |
          set -e
          APP_DOMAIN="${{ steps.aks_output.outputs.dns_subdomain }}.${{ steps.aks_output.outputs.dns_zone_name }}"
          INGRESS_IP=$(kubectl -n ingress-nginx get svc ingress-nginx-controller -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
          echo "Ingress IP: $INGRESS_IP"
          echo "FQDN: $APP_DOMAIN"

          if az network dns record-set a show -g "${{ steps.aks_output.outputs.dns_zone_rg }}" -z "${{ steps.aks_output.outputs.dns_zone_name }}" -n "${{ steps.aks_output.outputs.dns_subdomain }}" >/dev/null 2>&1; then
            az network dns record-set a update -g "${{ steps.aks_output.outputs.dns_zone_rg }}" -z "${{ steps.aks_output.outputs.dns_zone_name }}" -n "${{ steps.aks_output.outputs.dns_subdomain }}" --set "aRecords=[]"
          else
            az network dns record-set a create -g "${{ steps.aks_output.outputs.dns_zone_rg }}" -z "${{ steps.aks_output.outputs.dns_zone_name }}" -n "${{ steps.aks_output.outputs.dns_subdomain }}" --ttl 60
          fi
          az network dns record-set a add-record -g "${{ steps.aks_output.outputs.dns_zone_rg }}" -z "${{ steps.aks_output.outputs.dns_zone_name }}" -n "${{ steps.aks_output.outputs.dns_subdomain }}" -a "$INGRESS_IP"

      
      - name: Apply monitoring resources (Grafana datasource + dashboard)
        run: |
          set -e
          # Ensure monitoring namespace exists
          kubectl get ns monitoring || kubectl create ns monitoring
          # Apply datasource first so Grafana picks it up on start
          kubectl apply -f k8s/monitoring/grafana-datasource.yml -n monitoring
          # Restart Grafana to reload provisioning
          kubectl -n monitoring rollout restart deploy/kube-prometheus-stack-grafana || true
          # Re-apply dashboard after restart to ensure bindings
          kubectl apply -f k8s/monitoring/grafana-dashboard.yml -n monitoring

      - name: Show Grafana admin password
        run: |
          echo "Grafana admin password (kube-prometheus-stack-grafana):"
          kubectl -n monitoring get secret kube-prometheus-stack-grafana -o jsonpath='{.data.admin-password}' | base64 --decode ; echo

      - name: Wait for rollouts
        run: |
          set -e
          kubectl rollout status deployment/backend-deploy -n dev --timeout=180s
          kubectl rollout status deployment/frontend-deploy -n dev --timeout=180s

      - name: Verify deployments
        run: |
          kubectl get pods -A
          kubectl get svc -A
          kubectl get ingress -A

      - name: Debug routing and backend health
        run: |
          kubectl -n dev describe ingress backend-ingress || true
          kubectl -n dev describe ingress frontend-ingress || true
          INGRESS_IP=$(kubectl -n ingress-nginx get svc ingress-nginx-controller -o jsonpath='{.status.loadBalancer.ingress[0].ip}' || true)
          if [ -n "$INGRESS_IP" ]; then
            curl -sSI --max-time 8 "http://$INGRESS_IP/api/health" || true
          fi
