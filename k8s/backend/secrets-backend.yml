apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  labels:
    app: backend-secrets
type: Opaque
stringData:
  db-password: "YourStrong!Passw0rd"
---
# Note: For production, use sealed secrets or external secret management
# Never commit actual secrets to version control
# Use: kubectl create secret generic backend-secrets --from-literal=db-password='your-password' --dry-run=client -o yaml | kubectl apply -f -

