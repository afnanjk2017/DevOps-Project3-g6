apiVersion: v1
kind: Secret
metadata:
  name: frontend-secrets
  labels:
    app: frontend-secrets
type: Opaque
stringData:
  # Add any frontend secrets here if needed
  # For example: api-key: "your-api-key"
---
# Note: For production, use sealed secrets or external secret management
# Never commit actual secrets to version control


